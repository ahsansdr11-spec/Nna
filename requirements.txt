flask==3.1.3
yt-dlp>=2026.7.4
requests==2.33.0
gunicorn==21.2.0
pillow>=10.0.0   # konversi gambar & enhancer foto (fitur Convert + HD Enhancer)
curl_cffi==0.11.4   # PIN: >=0.14 tidak kompatibel dgn impersonate yt-dlp; jangan pkg build di Termux — pakai wheel aarch64
ytmusicapi>=1.12.0
gallery-dl>=1.32.0
yt-dlp-ejs>=0.8.0   # skrip JS solver challenge utk PO token lokal (dipakai bareng runtime deno) — tanpa cookie
xhshow>=0.1.0        # tanda tangan x-s/x-t/x-rap-param RedNote (pure Python)
